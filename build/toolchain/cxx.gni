declare_args() {
  # gcc or clang
  cc = "gcc"

  # g++ or clang++
  cxx = "g++"

  # C language version, e.g. c17
  std_c = "c17"

  # C++ language version, e.g. c++17
  std_cc = "c++17"

  # extra flags.
  extra_cflags_c = ""
  extra_cflags_cc = ""
  extra_ldflags = ""

  # extra defines.
  extra_defines_c = ""
  extra_defines_cc = ""

  # debug or release
  build_type = "debug"
}
